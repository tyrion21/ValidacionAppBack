# Database URLs for Prisma
# Replace with your actual SQL Server connection strings

# Schema4 - Main database
DATABASE_URL="sqlserver://username:password@server:port;database=DatabaseName;trustServerCertificate=true"

# Schema2 database
DATABASE_URL2="sqlserver://username:password@server:port;database=DatabaseName2;trustServerCertificate=true"

# Schema3 database  
DATABASE_URL3="sqlserver://username:password@server:port;database=DatabaseName3;trustServerCertificate=true"

# Schema5 database (uses DATABASE_URL4 in schema)
DATABASE_URL4="sqlserver://username:password@server:port;database=DatabaseName4;trustServerCertificate=true"

# Application configuration
NODE_ENV=production
PORT=3000